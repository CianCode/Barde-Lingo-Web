version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: barde_lingo:latest
    container_name: barde_lingo_app
    restart: unless-stopped
    working_dir: /var/www
    expose:
      - "9000"
      - "8080"
    volumes:
      - ./storage:/var/www/storage
      - ./.env:/var/www/.env
      - app_public:/var/www/public
    networks:
      - barde_lingo_network
    depends_on:
      - redis
    environment:
      - APP_ENV=production
      - CONTAINER_ROLE=app
    healthcheck:
      test: ["CMD-SHELL", "php-fpm-healthcheck || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3

  nginx:
    image: nginx:alpine
    container_name: barde_lingo_nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
      - "8080:8080"
    volumes:
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
      - ./docker/nginx/reverb.conf:/etc/nginx/conf.d/reverb.conf
      - app_public:/var/www/public:ro
      - nginx_logs:/var/log/nginx
      - nginx_cache:/var/cache/nginx
    networks:
      - barde_lingo_network
    depends_on:
      - app
    healthcheck:
      test: ["CMD-SHELL", "wget --quiet --tries=1 --spider http://localhost/robots.txt || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3

  redis:
    image: redis:7-alpine
    container_name: barde_lingo_redis
    restart: unless-stopped
    expose:
      - "6379"
    volumes:
      - redis_data:/data
    networks:
      - barde_lingo_network
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 10s
      retries: 3

  alloy:
    image: grafana/alloy:latest
    container_name: barde_lingo_alloy
    restart: unless-stopped
    command: run --server.http.listen-addr=0.0.0.0:12345 --storage.path=/var/lib/alloy/data /etc/alloy/config.alloy
    ports:
      - "12345:12345"
    volumes:
      - ./docker/alloy/config.alloy:/etc/alloy/config.alloy:ro
      - ./storage/logs:/var/log/laravel:ro
      - nginx_logs:/var/log/nginx:ro
      - alloy_data:/var/lib/alloy/data
    networks:
      - barde_lingo_network
    environment:
      - HOSTNAME=${HOSTNAME:-barde_lingo_vm}
    depends_on:
      - nginx
      - app

networks:
  barde_lingo_network:
    driver: bridge

volumes:
  redis_data:
    driver: local
  app_public:
    driver: local
  nginx_cache:
    driver: local
  nginx_logs:
    driver: local
  alloy_data:
    driver: local
